{
  "name": "mmw-sync",
  "version": "1.1.0",
  "private": true,
  "description": "Cloudflare Worker + D1 cloud-sync backend for Caring Hands.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test-worker.mjs",
    "migrate:local": "wrangler d1 migrations apply mmw --local",
    "migrate:remote": "wrangler d1 migrations apply mmw --remote"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
